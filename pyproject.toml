[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lookahead-free"
version = "0.1.3"
description = "Verifiable look-ahead checks for value-independent data pipelines: decision availability, windows, PIT reads, joins, monotonicity, and explicit heuristic boundaries."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = ["lookahead", "point-in-time", "leakage", "backtest", "verification", "pipeline", "quant"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Investment",
    "Topic :: Scientific/Engineering",
]

[project.scripts]
lf = "lookahead_free.cli:main"
lookahead-free = "lookahead_free.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

